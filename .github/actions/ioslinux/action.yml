name: "IOS Linux Build"
description: "IOS Linux Build"
runs:
  using: "composite"
  steps:
    - name: Install Monorepo Dependencies
      run: yarn install --frozen-lockfile
      shell: bash

    # FIX 1: Compiles root source modules so example package cross-linking doesn't break
    - name: Precompile Root Component Package
      run: yarn prepare 
      shell: bash

    - name: Install Example Application Packages
      run: |
        cd example
        yarn install --frozen-lockfile
      shell: bash

    - name: Install CocoaPods Native Dependencies
      run: |
        cd example/ios
        pod install
      shell: bash

    - name: Clean Project Workspaces
      run: |
        cd example/ios
        xcodebuild clean -workspace example.xcworkspace -scheme example -configuration Release
      shell: bash

    - name: Generate Production JavaScript Offline Bundle
      run: |
        cd example
        npx react-native bundle \
          --entry-file index.js \
          --platform ios \
          --dev false \
          --bundle-output ios/main.jsbundle \
          --assets-dest ios/
      shell: bash

    - name: Compile Standalone Simulator Binary Archive (Release Build)
      run: |
        xcodebuild \
          -workspace example/ios/example.xcworkspace \
          -scheme example \
          -configuration Release \
          -sdk iphonesimulator \
          -derivedDataPath example/ios/build \
          ONLY_ACTIVE_ARCH=NO \
          SKIP_BUNDLING=false \
          UNLOCALIZED_RESOURCES_FOLDER_PATH="example.app"
      shell: bash

    - name: Force Embed Bundle variations into App Package Structure
      run: |
        TARGET_APP_DIR="example/ios/build/Build/Products/Release-iphonesimulator/example.app"
        cp example/ios/main.jsbundle "$TARGET_APP_DIR/main.jsbundle"
        cp example/ios/main.jsbundle "$TARGET_APP_DIR/index.jsbundle"
      shell: bash

    - name: Compress App for Distribution Artifacts
      run: |
        mkdir -p build-output
        cp -R example/ios/build/Build/Products/Release-iphonesimulator/example.app build-output/
        cd build-output && zip -r ios-simulator-build.zip example.app
      shell: bash

    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ios-simulator-app
        path: build-output/ios-simulator-build.zip